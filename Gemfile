source "https://rubygems.org"

# Plain Jekyll — installs cleanly on modern Ruby and is fine for local preview.
# GitHub Pages builds the site with its own Jekyll regardless of this file,
# and this theme uses no custom plugins, so output is identical.
gem "jekyll", "~> 4.3"
gem "webrick"          # required by Ruby 3+ for `jekyll serve`
